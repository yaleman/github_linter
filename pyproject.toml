[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "github_linter"
authors = [
    { name = "James Hodgkinson", email = "james@terminaloutcomes.com" }
]
classifiers = ["License :: OSI Approved :: MIT License"]

readme = "README.md"
dynamic = [
    "version",
    "description"
]

dependencies = [
    "click",
    "PyGithub",
    "requests",
    "pyyaml",
    "loguru",
]

keywords = [
    "github"
]

[project.urls]
Home = "https://github.com/yaleman/github_linter"


[project.optional-dependencies]
dev = [
    "flit",
    "pytest",
    "pylint",
    "mypy",
    "types-PyYAML",
]

[project.scripts]
fileshimstem = "github_linter.__main__:run"


[tool.flit.sdist]
exclude = [
    "requirements*.txt",
    ".gitignore",
    ".pylintrc",
    "*.json.example*",
]